# Decisions log

A running log of what was tried, what worked, what didn't, and where Claude's
leverage was high vs low. This is the actual output of the experiment (see
`CLAUDE.md` → Definition of done).

| Date | Decision / experiment | Outcome | Notes |
|------|-----------------------|---------|-------|
| 2026-06-10 | Initial repo scaffold + devcontainer (pac CLI, .NET, Node, PowerShell) | Set up | Connected to github.com/ionyzeme/ionsales; CLAUDE.md = briefing. |
| 2026-06-10 | Connected to Dataverse env `org27cdc0fd.crm11.dynamics.com` (org **ESP-PO**, tenant `837997c0…`) via dv-connect skill | Connected | Authed as Mark.Eburne@ionyze.co.uk on both DV CLI + PAC profile `esppo`. Env confirmed Sales+PO (2015 tables; opportunity/msdyn_project/quote present; 0 opps, 1 project). |
| 2026-06-10 | Gotcha: `dataverse auth create --deviceCode` prints nothing through a plain pipe; PAC `auth create` **crashes** under a `script` pseudo-TTY (ArgumentOutOfRangeException reading console) | Worked around | Run DV CLI device-code under a PTY (`script -qefc`), run PAC device-code with **plain** redirection (no PTY). |
| 2026-06-10 | Gotcha: headless container has no keyring, so DV CLI writes a **plaintext** MSAL cache (`~/.local/share/Microsoft/DataverseCli/tokencache_msalv3.dat`); `scripts/auth.py` Linux branch used `LibsecretPersistence` → 0 accounts → silent device-code hang | Patched `scripts/auth.py` | Linux branch now prefers the plaintext file via `FilePersistence` when it exists, falling back to libsecret. Python token reuse is now silent. |
| 2026-06-10 | **Slice chosen: Sales Intelligence Brief on `account`** (manual to start). Reused existing **`ion_`** publisher (not a fresh `inz_`); solution **`ion_SalesPilot`** created under it. | Built | Decision drivers: brief sits on the customer (account) record; content typed by a human for now, AI/flow population bolted on later. Updated CLAUDE.md + `.env` from `inz_`→`ion_`. |
| 2026-06-10 | Added 3 segmented columns to `account`: `ion_brief` (Memo 4000), `ion_accounthealth` (Choice: Healthy/Watch/At Risk), `ion_briefupdatedon` (DateTime). Surfaced on **Account** main form via a new "Sales Intelligence" tab; published. | Done, in solution | `MSCRM.SolutionName` header was **silently ignored** on the system `account` table (0 components) — had to add the segmented entity + each column + the form via explicit `AddSolutionComponent`. Lesson: always verify solution membership after header-based adds. |
| 2026-06-10 | Enforcement built as a **C# plug-in** (not a portal business rule) — user chose the all-code/no-portal path | **Done, tested live** | `Ion.SalesPilot.Plugins.AccountBriefGuard`, sync pre-operation on account Create+Update. (1) blocks save when `ion_accounthealth=At Risk` and `ion_brief` empty; (2) auto-stamps `ion_briefupdatedon` on brief change. Where Claude's leverage is **high** — deterministic C# beats fragile BR XAML, and it's enforced server-side regardless of entry point. |
| 2026-06-10 | Plug-in toolchain is fully headless on Linux | Works | `pac plugin init` scaffolds a signed net462 class lib **and generates the `.snk`** (no Windows/`sn`/mono needed); `dotnet build -c Release` compiles; assembly + plugintype + 2 steps + pre-image registered via **Web API** (sourcetype=0, isolationmode=2 sandbox); strong-name identity read via `AssemblyName.GetAssemblyName`. No Plug-in Registration Tool. Live tests: At-Risk-no-brief→blocked, At-Risk+brief→created & date stamped, Healthy→created, Healthy→At-Risk-no-brief update→blocked (pre-image works). |
| 2026-06-10 | ALM nuance: plug-in `.snk` is **gitignored** (`*.snk`) | Note / open question | The committed `solutions/.../PluginAssemblies/*.dll` is the deployable artifact, so `pac solution pack`+import rebuilds fine **without** the key. The `.snk` only matters for recompiling the C# from source (a fresh build would mint a new public-key token). For full source-rebuildability, commit the dev `.snk` (acceptable for a throwaway sandbox key) — left gitignored for now to respect the existing secrets policy. |
| 2026-06-10 | Pulled `ion_SalesPilot` to repo via `pac solution export --managed false` + `unpack` | In git tree | **Finding:** `pac unpack` (and `clone`) emit the verbose **SolutionPackager XML** format, not the "YAML source-control" format CLAUDE.md anticipated — that YAML is a separate Power Platform Git-integration feature, not a `pac` flag. CLAUDE.md's Layer-2 description may need correcting. Slice verified captured (columns, option labels, form tab all present). Not yet committed. |
