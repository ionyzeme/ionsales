# Schema digest — Sales Pilot slice (Layer 3b)

> The most important file to maintain. A hand/AI-curated map of *just our slice*,
> not the 4,000-table managed model. Generate/extend it via the Dataverse MCP
> (`describe` each table) once connected. See `CLAUDE.md` → Layer 3.

_Empty until the MCP is connected and the thin slice is chosen. Suggested seed
prompt (from `CLAUDE.md`):_

> Using the Dataverse MCP, `describe` the `opportunity`, `msdyn_project`,
> `msdyn_projectteam`, and `quote` tables. For each, write: purpose, the columns
> I'll use (logical name, type, required), key relationships, relevant choice
> sets, and any business process flows that touch them. Mark out-of-box vs custom.

## Tables

_TBD_
