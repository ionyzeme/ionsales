# --- Secrets / connection details (never commit) ---
.env
.devcontainer/devcontainer.env
**/appsettings.local.json
*.pfx
*.snk
# Exception: the disposable-sandbox plug-in dev signing key is committed so the
# C# assembly is rebuildable from source with a stable public-key token. This is
# a throwaway dev key for an experiment env — NOT a production signing key.
!src/plugins/Ion.SalesPilot.Plugins/Ion.SalesPilot.Plugins.snk

# --- Power Platform CLI ---
.pac/
bin/
obj/
out/
*.zip            # exported solution archives (rebuild via `pac solution pack`)
!**/.gitkeep

# --- PCF / Node (Layer 2 src/pcf) ---
node_modules/
.pcf-scripts/
*.tsbuildinfo

# --- .NET (plugins) ---
*.user
*.suo
.vs/

# --- OS / editor noise ---
.DS_Store
Thumbs.db
.token_cache.bin
__pycache__/
*.pyc
